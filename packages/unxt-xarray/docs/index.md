# unxt-xarray (deprecated)

`unxt-xarray` has been superseded by `unxts.xarray`.

Migrate: replace `pip install unxt-xarray` with `pip install unxts.xarray` and `import unxt_xarray` with `import unxts.xarray`.

**→ [unxts.xarray documentation](https://unxt.readthedocs.io/en/latest/packages/unxts.xarray/)**
