# unxt-api (deprecated)

`unxt-api` is a backward-compatible shim for `unxts.api`.

No code changes are required — `import unxt_api` continues to work. For documentation, API reference, and extension guides, see the canonical package:

**→ [unxts.api documentation](https://unxt.readthedocs.io/en/latest/packages/unxts.api/)**
