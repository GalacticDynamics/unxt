# unxt-hypothesis (deprecated)

`unxt-hypothesis` is a backward-compatible shim for `unxts.hypothesis`.

No code changes are required — `import unxt_hypothesis` continues to work. For documentation, strategies reference, and examples, see the canonical package:

**→ [unxts.hypothesis documentation](https://unxt.readthedocs.io/en/latest/packages/unxts.hypothesis/)**
