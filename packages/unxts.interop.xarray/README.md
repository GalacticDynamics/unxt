# unxts.interop.xarray

xarray integration for [unxt](https://github.com/GalacticDynamics/unxt).

This is the canonical package (`unxts.interop.xarray`). It provides xarray accessors and conversion utilities for unxt's JAX-based quantities.

## Install

```bash
pip install unxts.interop.xarray
```

## Usage

```python
import unxts.interop.xarray
```
